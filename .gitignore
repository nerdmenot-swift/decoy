.DS_Store
/.build/
/Packages/
/*.xcodeproj
xcuserdata/
DerivedData/
.swiftpm/
Package.resolved

# Adapter workspace. `out/` is regenerable from the adapters; `.cache/` holds verified
# upstream tarballs, which are reproducible from the pinned integrity hashes.
/Tools/adapters/out/
/Tools/adapters/.cache/

# The compiled corpus IS committed — see Corpus/README.md. It was ignored as "a build
# artifact, reproducible from the adapters", which was true and stopped being enough:
# reproducing it needs 49 upstreams reachable, and two already are not.

# proto toolchain install logs
proto-*.log
/.website-extract/

# Never committed. The root package.json that pulled one in was a stray, and the
# website keeps its own under website/.
node_modules/
