.DS_Store
/.build/
/Packages/
/*.xcodeproj
xcuserdata/
DerivedData/
.swiftpm/
Package.resolved

# Extractor workspace
node_modules/
/Tools/extractor/out/

# Compiled corpus — a build artifact, reproducible from the extractor output.
# Revisit when per-locale targets need blobs committed for distribution.
/Corpus/binary/

# proto toolchain install logs
proto-*.log
