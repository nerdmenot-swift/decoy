.DS_Store
/.build/
/Packages/
/*.xcodeproj
xcuserdata/
DerivedData/
.swiftpm/
Package.resolved

# Adapter workspace. `out/` is regenerable from the adapters; `.cache/` holds verified
# upstream tarballs, which are reproducible from the pinned integrity hashes.
/Tools/adapters/out/
/Tools/adapters/.cache/

# Compiled corpus — a build artifact, reproducible from the adapters.
# Revisit when per-locale targets need blobs committed for distribution.
/Corpus/binary/

# proto toolchain install logs
proto-*.log
/.website-extract/
